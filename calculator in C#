
public class Calc : InterfaceCalc
{
    private double a = 0;
    private double memory = 0;
    private double b=0;
    private double memory = 0;
    private double c=0;
    private double memory = 0;

    public void Put_A(double a)
  public void Put_B(double b)
  public void Put_Cdouble c)
    {
        this.a = a;
        this.b=b;
        this.c=c;
    }
 /// Declaring variables
    public void Clear_A()
public void Clear_B()
    {
        a = 0;
        b=0
    }
 
    public double Multiplication(double b)
    {
        return a * b;
    }
 // We make sure that the multiplication operation is performed
    public double Division(double b)
    {
        return a / b;
    }
//We make sure that the division operation is performed
    public double Sum(double b)
    {
        return a + b;
    }
///We make it so that the addition operation is performed
    public double Subtraction(double b) //вычитание
    {
        return a - b;
    }
 /// We make sure that the subtraction operation is performed
    public double SqrtX(double b)
    {
        return Math.Pow(a, 1 / b);
    }
 
    public double DegreeY(double b)
    {
        return Math.Pow(a, b);
    }
 
    public double Sqrt()
    {
        return Math.Sqrt(a);
    }
 
    public double Square()
    {
        return Math.Pow(a, 2.0);
    }
 return.math.Pow(0,b,2)
}
return.math.Pow(0,2,c)
    public double Factorial();
    pubclic double Module ();
    public double a++();
    public double a--();
    {
// we use the Increment operation
        double f = 1;
 
        for (int i = 1; i <= a; i++)
            f *= (double)i;
 
        return f;
    }
 // We use the FOR loop and then return the value we need
    //show the contents of the mother's register
    public double MemoryShow()
    {
        return memory;
    }
 
    //erase the contents of the memory register
    public void Memory_Clear()
    {
        memory = 0.0;
    }
 
    //* / + -to the memory register
 
    public void M_Subtraction(double b)
    {
        memory -= b;
    }
 
}
// We are doing an operation that increases the value several times
